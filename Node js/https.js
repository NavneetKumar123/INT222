//const { log } = require('console');
const http= require('http');
const  server=htpps.createServer((req,res)=>{
    res.end("Hello")
    
})
//const server=http.createServer(reqListener);
server.listen(3000);